import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.qrtoapp',
  appName: 'QRtoApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
